import ModelsPageContent from "@/components/models/ModelsPageContent";

export const metadata = {
  title: "Models — The Walk",
  description:
    "Browse The Walk Agency model roster — signature faces for runway, editorial, and commercial bookings in Sri Lanka.",
};

export default function ModelsPage() {
  return <ModelsPageContent />;
}
